# Backtester

Historical replay must invoke the same signal, consensus, and risk contracts as paper execution. Record costs, spread, slippage, data gaps, and all configuration versions with every result.
